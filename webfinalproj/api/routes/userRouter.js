const express = require('express');
const profileAbout = express.Router();
const postController = require('../controllers/postController');

// postRouter.get('/getallposts', postController.getallposts);

// module.exports = postRouter;